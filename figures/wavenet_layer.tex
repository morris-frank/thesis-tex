\begin{tikzpicture}
    \matrix[column sep=1.25mm, row sep=2mm]
    {
        & & \node[draw] (g)  {g}; &
        \node[draw] (gs) {\(\σ\)}; & & & & &\\

        \node (featold) {}; &
        \node (dilate) {dilate}; & & &
        \node (times)  {\(\odot\)}; & &
        \node[draw] (chfeat) {\(1\× 1\)}; &
        \node (plfeat) {+}; &
        \node (featnew) {};\\

        & & \node[draw] (f) {f}; &
        \node[draw] (ft)  {\(\tanh\)}; & &
        \node[draw] (chskip) {\(1\× 1\)}; & & &\\

        \node (skipold) {}; & \node (skip) {skip}; & & & &
        \node (plskip) {+}; & & &
        \node (skipnew) {};\\
    };
    \begin{scope}[every path/.style={draw, ->, semithick}]
        \path (featold) -- (dilate);
        \path (dilate) |- (g);
        \path (dilate) |- (f);
        \path (g) -- (gs);
        \path (f) -- (ft);
        \path (gs) -| (times);
        \path (ft) -| (times);
        \path (times) -- (chfeat);
        \path (chfeat) -- (plfeat) -- (featnew);
        \path (times) -| (chskip);
        \path (chskip) -- (plskip);

        \path (dilate) |- (1,1.4) -| (plfeat);

        \path (skipold) -- (skip) -- (plskip) -- (skipnew);
    \end{scope}
\end{tikzpicture}
