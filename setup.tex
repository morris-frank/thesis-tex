\usepackage{ifxetex}

\usepackage{morris}

%Frontmatter
\renewcommand{\maketitlepage}[0]{%
  \cleardoublepage%
  {%
  \begin{fullwidth}%
  \fontsize{18}{20}\selectfont\par\noindent\thanklessauthor%
  \vspace{11.5pc}%
  \fontsize{18}{40}\selectfont\par\noindent\thanklesstitle%
  \vfill%
  \fontsize{14}{16}\selectfont\par\noindent\allcaps{\thanklesspublisher}%
  \end{fullwidth}%
  }
  \clearpage%
}

% Geometry
%\usepackage{typearea} % wider textarea
%\usepackage{savetrees} % really wide textarea! save them trees
\usepackage{tcolorbox}

% Typography
\frenchspacing % space between sentences as between words
%\usepackage[final,factor=1100,stretch=10,shrink=10]{microtype}

\ifxetex
  \newcommand{\textls}[2][5]{%
    \begingroup\addfontfeatures{LetterSpace=#1}#2\endgroup
  }
  \renewcommand{\allcapsspacing}[1]{\textls[15]{#1}}
  \renewcommand{\smallcapsspacing}[1]{\textls[10]{#1}}
  \renewcommand{\allcaps}[1]{\textls[15]{\MakeTextUppercase{#1}}}
  \renewcommand{\smallcaps}[1]{\smallcapsspacing{\scshape\MakeTextLowercase{#1}}}
  \renewcommand{\textsc}[1]{\smallcapsspacing{\textsmallcaps{#1}}}
\fi

\usepackage{xcolor}
\hypersetup{
    colorlinks,
    linkcolor={red!40!black},
    citecolor={green!50!black},
    urlcolor={blue!50!black}
}

% Referencing
\setcounter{tocdepth}{1} % Level in TOC

\usepackage{hyphenat} % only with tufte
\usepackage[
    %style=ieee,
    hyperref, backref,
    sortcites,
    date=year, sorting=nyt,
    isbn=false, doi=false, url=false,
    %maxcitenames=2, mincitenames=1,
    %maxbibnames=10, minbibnames=10,
    dashed=false,
    style=verbose,
    autocite=footnote,
    backend=biber
  ]{biblatex}
\def\bibfont{\small}
\addbibresource{References.bib}

\usepackage[capitalise, noabbrev]{cleveref}

% Graphics and Figures
\setkeys{Gin}{width=\linewidth,totalheight=\textheight,keepaspectratio}
\graphicspath{{figures/}} % set of paths to search for images

\usepackage{tikz}
\usepackage{tikz-bayesnet}
\usetikzlibrary{calc}
\usetikzlibrary{graphs}
\usepackage{forest}

\input{macros.tex}
